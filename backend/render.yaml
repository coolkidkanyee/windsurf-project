services:
  - type: web
    name: 21-online-backend
    env: node
    plan: starter
    buildCommand: npm run build
    startCommand: npm run start-prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
